<div class="coller-home">
<h3> Comments </h3>
<hr>

<% @post.comments.each do |comment| %>
    <div class="card">
        <div class="card-body">
            <%= comment.commenter %><hr>
            <%= comment.body %>
        </div>
    </div>
    <br>
<% end %>
